{
  "name": "typescript-learnign",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "serve": "live-server mainapp/public --port=5500 --no-browser",
    "dev": "npm run build && npm run serve"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "typescript": "^5.8.3"
  }
}
